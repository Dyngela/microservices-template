spring:
  application:
    name: product
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/product
    username: postgres
    password: gernika54
server:
  port: 8009
