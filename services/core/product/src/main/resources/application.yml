#spring:
#  application:
#    name: product
#  config:
#    import: "optional:configserver:"
#  cloud:
#    config:
#      fail-fast: true
#      discovery:
#        enabled: true
#        service-id: config-server
#      retry:
#        initialInterval: 2000
#        multiplier: 1.5
#        maxInterval: 60000
#        maxAttempts: 100
#
#eureka:
#  client:
#    service-url:
#      enabled: true
#      fetch-registry: true
#      register-with-eureka: true
#      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
#  instance:
#    lease-renewal-interval-in-seconds: 10

server:
  port: 8009
  servlet:
    context-path: /product/api/v1/

spring:
  application:
    name: product
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/product
    username: postgres
    password: gernika54

